import assert from 'node:assert/strict';
import {readFileSync} from 'node:fs';

const html=readFileSync('index-grupal.html','utf8');
assert.match(html,/id="clearAllRegistration">BORRAR TODO<\/button>/);
assert.match(html,/id="clearRoundScores"[^>]*>BORRAR TODO<\/button>/);
assert.doesNotMatch(html,/>\s*BORRAR SCORES\s*</,"Ninguna tarjeta activa puede conservar BORRAR SCORES");
const clearStart=html.indexOf("function clearAllRegistrationPlayers(){"),clearEnd=html.indexOf("function courseRatingText",clearStart),clearSource=html.slice(clearStart,clearEnd);
assert.match(clearSource,/window\.confirm\("¿CONFIRMAS BORRAR JUGADORES, SCORES Y RONDA ACTIVA\?/);
assert.match(clearSource,/deleteRoundFromArchive\(removedRoundId\)/,"BORRAR TODO debe retirar la ronda actual del Historial para que no reaparezca");
for(const key of ["ACTIVE_ROUND_KEY","STORAGE_KEY","STORAGE_BACKUP_KEY","MATCH_PLAY_ACTIVE_KEY","FOUR_BALL_ACTIVE_KEY","STABLEFORD_ACTIVE_KEY"])assert.match(clearSource,new RegExp(key),`BORRAR TODO debe limpiar ${key}`);
assert.match(clearSource,/localStorage\.setItem\(PRINCIPAL_RESET_KEY,"1"\)/);
assert.match(clearSource,/round=blankRound\(\)/);
assert.match(clearSource,/draftPlayers=\[\];[\s\S]*?manualDraftRows=Array\.from\(\{length:6\},\(\)=>emptyManualDraftRow\(\)\);[\s\S]*?clearDraftState\(\);[\s\S]*?renderDraft\(\);/);
assert.match(html,/\$\("clearAllRegistration"\)\.addEventListener\("click",clearAllRegistrationPlayers\)/);
assert.match(html,/clearButton\.onclick=\(\)=>\{if\(locked\)return;clearAllRegistrationPlayers\(\)\}/);
assert.match(html,/manual\.querySelector\("#roundManualTitle"\)\.textContent=`CONTROL MANUAL · \$\{stable\?"STABLEFORD":isFourBallRound\(\)\?"FOUR BALL":isMatchPlayRound\(\)\?"MATCH PLAY":isUniversalesRound\(\)\?"UNIVERSALES":"GENERAL"\}`/,
  "General, Stableford, Match Play, Four Ball y Universales deben compartir el mismo render de tarjeta");
assert.match(clearSource,/openSetup\("new"\)/,"Tras confirmar debe mostrar Inicio limpio");
assert.doesNotMatch(clearSource,/localStorage\.removeItem\(ROUND_ARCHIVE_KEY\)/,"otras rondas oficiales permanecen en Historial");
assert.match(html,/function deleteRoundFromArchive\(roundId\)[\s\S]*?deleted\.add\(id\)/,"la ronda queda marcada aunque todavía no estuviera archivada");
assert.match(html,/const deleted=readRoundArchiveDeleted\(\),restored=[\s\S]*?filter\(item=>item&&!deleted\.has\(String\(item\.id\)\)\)/,"la recuperación central no revive una ronda eliminada");
assert.match(html,/document\.body\?\.classList\.add\("gsc-final-card-open"\)/);
assert.match(html,/document\.body\?\.classList\.remove\("gsc-final-card-open"\)/);
for(const control of ['live-support-link','ai-universal-launch','golf-rules-launch','skins-launch','gsc-live-launch'])assert.match(html,new RegExp(`body\\.gsc-final-card-open \\.${control}`));
assert.match(html,/body:has\(\.overlay\.visible\) \.mandatory-update\{display:none!important\}/,"ACTUALIZADO no debe cubrir ATRÁS ni acciones dentro de pantallas");
assert.match(html,/#finalCardOverlay \.final-card-panel\{width:100%;min-width:0/);
assert.match(html,/#finalCardOverlay \.final-card-shell\{width:100%;max-width:100%;overflow-x:auto/);
assert.match(html,/#finalCardOverlay \.final-card-head>div\{display:grid;grid-template-columns:repeat\(3,minmax\(0,1fr\)\)/);
console.log('PASS V405 · BORRAR TODO elimina ronda activa, evita reaparición y conserva otras rondas del Historial.');
