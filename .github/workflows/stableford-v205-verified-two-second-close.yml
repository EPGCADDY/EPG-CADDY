name: Stableford V205 verified two second close
on:
  push:
    branches: [stableford-tournament-final]
    paths: ['.github/workflows/stableford-v205-verified-two-second-close.yml']
permissions:
  contents: write
jobs:
  patch-audit-commit:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: stableford-tournament-final
          fetch-depth: 0
      - name: Patch exact active close timer
        shell: python
        run: |
          from pathlib import Path
          p=Path('index-grupal.html'); s=p.read_text(encoding='utf-8')
          old='roundFinalizeTimer=setTimeout(()=>{roundFinalizeTimer=null;requestRoundFinalize(0,true)},4000);'
          new='roundFinalizeTimer=setTimeout(()=>{roundFinalizeTimer=null;requestRoundFinalize(0,true)},(typeof round!=="undefined"&&round&&round.mode==="stableford")?2000:4000);'
          if s.count(old)!=1: raise SystemExit(f'guard active close timer count={s.count(old)}')
          s=s.replace(old,new,1)
          p.write_text(s,encoding='utf-8')
      - name: Audit frozen scope and tests
        shell: bash
        run: |
          set -euo pipefail
          git diff --check
          test "$(git diff --name-only)" = "index-grupal.html"
          grep -Fq 'round.mode==="stableford")?2000:4000' index-grupal.html
          git diff --quiet HEAD -- api/session-grupal.js stableford.js stableford-torneo.html
          python - <<'PY'
          from pathlib import Path
          s=Path('index-grupal.html').read_text(encoding='utf-8')
          assert 'round.mode==="stableford")?2000:4000' in s
          js=s.split('<script>',1)[1].split('</script>',1)[0]
          Path('/tmp/g.js').write_text(js,encoding='utf-8')
          PY
          node --check /tmp/g.js
          node test-voice-continuity.mjs
          node test-stableford.mjs
          node test-stableford-ui.mjs
          node test-stableford-torneo.mjs
      - name: Commit patch only
        shell: bash
        run: |
          set -euo pipefail
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add index-grupal.html
          git commit -m 'Stableford V205 verified two-second round close'
          git push origin stableford-tournament-final