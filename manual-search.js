(function(root,factory){const api=factory();if(typeof module==="object"&&module.exports)module.exports=api;if(root)root.GSCManualSearch=api;})(typeof globalThis!=="undefined"?globalThis:this,function(){"use strict";function normalize(v){return String(v||"").toLowerCase().normalize("NFD").replace(/[\u0300-\u036f]/g,"").replace(/[^a-z0-9ñ]+/g," ").replace(/\s+/g," ").trim()}function search(query,entries,{limit=8}={}){const q=normalize(query);if(!q)return[];const tokens=q.split(" ").filter(Boolean);return(entries||[]).map(e=>{const h=normalize([e.title,e.label,e.searchText,e.category].join(" "));let score=h.includes(q)?100:0;for(const t of tokens)if(h.includes(t))score+=10;return{...e,score}}).filter(x=>x.score>0).sort((a,b)=>b.score-a.score).slice(0,limit)}return Object.freeze({normalize,search});});
